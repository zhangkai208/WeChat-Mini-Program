# 今日人设小程序 · 海报云存储实施计划（持久化 + 当天复用 + 历史回看）

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 把 Canvas 合成的最终海报图存进微信云开发「云存储」，当天生成一次后复用（不再每次调 AI 生图），并让历史记录每条点开能回看当天海报。

**Architecture:** 海报图存云存储 `posters/{recordId}.png`，fileID 挂到当天 `records` 记录的新字段 `posterFileID`。`genPosterImage` 云函数改造为按 `action` 分派（`getToday` 探缓存/生图，`save` 回写）。home 的「生成海报」走「探缓存 → 命中直接预览 / 未命中生图+合成+上传+回写 → 预览」。history card 加点击，有 `posterFileID` 就 `wx.previewImage`，否则占位提示。

**Tech Stack:** 微信小程序原生（WXML/WXSS/JS）、腾讯云开发（云函数 `wx-server-sdk` + `@cloudbase/node-sdk`、云数据库 `records`、云存储 `posters/`）、Canvas 2D、`wx.previewImage`（支持 `cloud://` fileID）。

## Global Constraints

- **平台**：微信小程序原生开发。
- **云存储权限**：`posters/` 用默认「所有用户可读，仅创建者可写」（上传者即创建者，自己能覆盖当天那张；别人读不到你的 record 也就拿不到 fileID）。
- **数据库权限**：`records` 集合保持「仅创建者可读写」不变。
- **_openid 安全红线**：服务端 `cloud.getWXContext()` 取，**不信任前端传**；`genPosterImage` 查/写 record 时 `where {_id, _openid}` 双校验，防伪造他人 recordId。
- **AI 调用不碰**：`generatePersona` 的 `app.ai().createModel('cloudbase')`、`genPosterImage` 的 `createImageModel('hunyuan-image')` 保持不变（node-sdk 环境鉴权，无 AI_KEY）。
- **验证约定**：开发者工具中运行 + 观察预期效果，不写自动化单测。
- **提交频次**：每个 Task 结束 commit。

## 验证与测试约定

- **云函数**：云开发控制台 → 云函数 → 测试，传 JSON 参数看返回；或前端调用后看结果。
- **云存储**：控制台 → 存储，看 `posters/` 下文件是否生成。
- **小程序页面**：开发者工具模拟器操作，观察表现。
- **每个任务的"验证"给出：操作 + 预期结果**。

---

## 文件结构（改动后）

云函数：
- `cloudfunctions/generatePersona/index.js` — OK 分支补返 `_id`
- `cloudfunctions/generatePosterImage/index.js` — 改造为 action 分派
- `cloudfunctions/genPosterImage/package.json` — 加 `wx-server-sdk` 依赖

小程序：
- `miniprogram/pages/home/home.js` — 重写 `onGenPoster`，删自画预览层相关
- `miniprogram/pages/home/home.wxml` — 删 `poster-mask` 预览层
- `miniprogram/pages/home/home.wxss` — 删预览层样式
- `miniprogram/pages/history/history.js` — 加 `onTapRecord`
- `miniprogram/pages/history/history.wxml` — card 加 `bindtap`

数据库：`records` 集合新增 `posterFileID` 字段（写入时自动生成，无需迁移）。
云存储：`posters/` 目录（首次上传后自动创建）。

---

## Task 1: generatePersona OK 分支补返 `_id`

**Files:**
- Modify: `cloudfunctions/generatePersona/index.js`

**Interfaces:**
- Produces: OK 分支返回的 `data` 多一个 `_id` 字段（record 的数据库 ID），供前端生成海报时定位记录、拼 cloudPath、回写。DUP_TODAY 分支不变（已含 `_id`）。

- [ ] **Step 1: add 接住返回值，return 时带上 `_id`**

把 `:62-78` 这段：
```js
  // 5. 写入 records（显式写 _openid，否则前端"仅创建者可读写"读不到）
  await db.collection('records').add({
    data: {
      _openid: OPENID,
      name, constellation, mood,
      persona: parsed.persona,
      fortune: parsed.fortune,
      yi: parsed.yi || [],
      ji: parsed.ji || [],
      luckScore: parsed.luckScore,
      date: today,
      createdAt: db.serverDate()
    }
  })

  // 6. 返回给前端
  return { code: 'OK', data: parsed }
```
改为：
```js
  // 5. 写入 records（显式写 _openid，否则前端"仅创建者可读写"读不到）
  const addRes = await db.collection('records').add({
    data: {
      _openid: OPENID,
      name, constellation, mood,
      persona: parsed.persona,
      fortune: parsed.fortune,
      yi: parsed.yi || [],
      ji: parsed.ji || [],
      luckScore: parsed.luckScore,
      date: today,
      createdAt: db.serverDate()
    }
  })

  // 6. 返回给前端（带 _id，供前端生成海报时定位记录、拼 cloudPath）
  return { code: 'OK', data: { ...parsed, _id: addRes._id } }
```

> 只改两处：`add` 前接 `const addRes =`、`return` 的 `data` 展开 `parsed` 并加 `_id`。AI 调用（`callAI`/`createModel`）、查重、DUP_TODAY 全部不动。

- [ ] **Step 2: 重新部署 + 测试**

开发者工具右键 `cloudfunctions/generatePersona` → 上传并部署。控制台 → 云函数 → `generatePersona` → 测试，输入：
```json
{ "name": "小明", "constellation": "白羊", "mood": "想摸鱼" }
```
**预期：** 返回的 `data` 里**多了 `_id` 字段**（一串数据库 ID）。

- [ ] **Step 3: 提交**
```bash
git add cloudfunctions/generatePersona/index.js
git commit -m "feat: generatePersona OK 分支补返 _id，供海报定位记录"
```

**验证：** 测试返回的 `data._id` 有值；前端首页生成后 `result._id` 能拿到（控制台 `console.log` 确认）。

---

## Task 2: genPosterImage 改造为 action 分派（getToday / save）

**Files:**
- Modify: `cloudfunctions/genPosterImage/index.js`
- Modify: `cloudfunctions/genPosterImage/package.json`

**Interfaces:**
- 入参 `{ action, recordId, constellation?, persona?, fileID? }`：
  - `action:'getToday'` + `{recordId, constellation, persona}` → 查当天 record：有 `posterFileID` 返 `{code:'CACHED', posterFileID}`；无则生图返 `{code:'OK', url}`。
  - `action:'save'` + `{recordId, fileID}` → 回写，返 `{code:'OK'}`。
- 服务端 `cloud.getWXContext()` 取 openid，`where {_id, _openid}` 双校验。

- [ ] **Step 1: `package.json` 加 wx-server-sdk 依赖**

```json
{
  "name": "genPosterImage",
  "version": "1.0.0",
  "description": "获取/保存今日人设海报（查缓存/生图/回写）",
  "main": "index.js",
  "dependencies": {
    "@cloudbase/node-sdk": "latest",
    "wx-server-sdk": "latest"
  }
}
```

- [ ] **Step 2: 重写 `index.js`（加 wx-server-sdk 取 openid + 操作 records；生图逻辑不变）**

```js
// 云函数：genPosterImage
// 职责（扩展）：按 action 分派——
//   getToday：查当天 record 有无 posterFileID，有就返缓存 fileID，没有就生图返背景图 url
//   save    ：把合成海报的 fileID 回写到当天 record
//
// 两个 SDK 并存（与 generatePersona 一致）：
//   wx-server-sdk       → getWXContext() 取 openid + 操作 records 数据库
//   @cloudbase/node-sdk → 调 AI 生图（createImageModel，走环境鉴权，不需要 AI_KEY）
//
// ⚠️ 返回的生图 url 只有 24 小时有效；前端拿到后必须立即 downloadFile 画进 Canvas。
const cloud = require('wx-server-sdk')
const tcb = require('@cloudbase/node-sdk')
cloud.init({ env: cloud.DYNAMIC_CURRENT_ENV })
const app = tcb.init({ env: 'zk-d2gcfqw9f402f9607', timeout: 150000 })

exports.main = async (event) => {
  const { action = 'getToday', recordId, constellation = '', persona = '', fileID } = event
  const { OPENID } = cloud.getWXContext()
  const db = cloud.database()

  // —— action === 'save'：回写 posterFileID ——
  if (action === 'save') {
    const upd = await db.collection('records')
      .where({ _id: recordId, _openid: OPENID })   // 双校验，防伪造他人 recordId
      .update({ data: { posterFileID: fileID } })
    if (upd.stats.updated === 0) return { code: 'NOT_FOUND', msg: '记录不存在或无权操作' }
    return { code: 'OK' }
  }

  // —— action === 'getToday'：探缓存，命中直接返，未命中生图 ——
  const r = await db.collection('records')
    .where({ _id: recordId, _openid: OPENID })
    .limit(1).get()
  if (!r.data.length) return { code: 'NOT_FOUND', msg: '记录不存在或无权操作' }
  if (r.data[0].posterFileID) {
    return { code: 'CACHED', posterFileID: r.data[0].posterFileID }
  }

  // 未命中：生图（沿用原逻辑）
  try {
    const prompt = buildPrompt(constellation, persona)
    const imageModel = app.ai().createImageModel('hunyuan-image')
    const res = await imageModel.generateImage({
      model: 'HY-Image-3.0-Plus-4090-Tob-v1.0',
      prompt,
      size: '720x1280',
      revise: { value: false }
    })
    const url = res.data[0] && res.data[0].url
    if (!url) return { code: 'AI_ERR', msg: 'AI 未返回图片' }
    return { code: 'OK', url }
  } catch (e) {
    return { code: 'AI_ERR', msg: '生图失败：' + e.message }
  }
}

// 12 星座专属色调（可爱治愈系插画配色），让每个星座背景有视觉区分度。
const CONSTELLATION_PALETTES = {
  '白羊': '暖珊瑚红与奶白色调',
  '金牛': '抹茶绿与燕麦米色调',
  '双子': '明柠檬黄与浅天蓝调',
  '巨蟹': '月光银蓝与薄雾紫调',
  '狮子': '落日金棕与琥珀橙调',
  '处女': '鼠尾草绿与米白色调',
  '天秤': '樱花粉与雾霾蓝调',
  '天蝎': '深葡萄紫与暗夜蓝调',
  '射手': '暖橙紫与落霞色调',
  '摩羯': '雾岩灰蓝与砂石灰调',
  '水瓶': '冰晶蓝与电光青调',
  '双鱼': '梦幻海蓝与樱花紫调'
}

function buildPrompt(constellation, persona) {
  const palette = CONSTELLATION_PALETTES[constellation] || '梦幻马卡龙多彩色调'
  const theme = constellation ? `${constellation}座主题元素与符号` : '梦幻星空元素'
  return [
    theme,
    '可爱治愈系插画风格',
    palette + '，柔和光影',
    '画面上半部分保留大面积纯色柔和留白，便于后续排版文字',
    '不要出现任何文字、字母、数字、签名或水印'
  ].join('，')
}
```

- [ ] **Step 3: 重新部署（云端安装依赖）**

开发者工具右键 `cloudfunctions/genPosterImage` → **上传并部署：云端安装依赖**（package.json 加了 wx-server-sdk，必须云端重装）。

- [ ] **Step 4: 测试三个分支**

控制台 → 云函数 → `genPosterImage` → 测试：
```json
// a) 一条还没海报的 record（先从数据库复制一个真实 _id 进来）
{ "action": "getToday", "recordId": "<某条 records 的 _id>", "constellation": "巨蟹", "persona": "测试" }
```
**预期：** 等十几秒返 `{code:'OK', url:'https://...'}`（生图）。

```json
// b) 回写
{ "action": "save", "recordId": "<同上 _id>", "fileID": "cloud://测试占位" }
```
**预期：** 返 `{code:'OK'}`，数据库该条多出 `posterFileID` 字段。

```json
// c) 再探缓存
{ "action": "getToday", "recordId": "<同上 _id>", "constellation": "巨蟹", "persona": "测试" }
```
**预期：** 秒返 `{code:'CACHED', posterFileID:'cloud://测试占位'}`（不再生图）。

- [ ] **Step 5: 提交**
```bash
git add cloudfunctions/genPosterImage
git commit -m "feat: genPosterImage 改造为 action 分派，支持缓存探测与海报回写"
```

**验证：** 三个分支返回符合预期；数据库 `posterFileID` 字段写入成功。

---

## Task 3: home 重写 onGenPoster + 删自画预览层

**Files:**
- Modify: `miniprogram/pages/home/home.js`
- Modify: `miniprogram/pages/home/home.wxml`
- Modify: `miniprogram/pages/home/home.wxss`

**Interfaces:**
- Consumes: 云函数 `genPosterImage`（`getToday`/`save`）、`wx.cloud.uploadFile`、`wx.previewImage`。

- [ ] **Step 1: home.js — data 删两个字段，删 closePoster 方法**

`data` 里删掉 `posterTempPath`、`posterVisible`，只保留 `posterLoading`：
```js
    posterLoading: false   // 海报生成中（生图云函数十几秒）
```
删掉整个 `closePoster() { ... }` 方法。

- [ ] **Step 2: home.js — 重写 onGenPoster**

```js
  // 点"生成海报"：先探缓存——命中直接预览；未命中生图→合成→上传云存储→回写→预览
  async onGenPoster() {
    const r = this.data.result
    if (!r) return
    if (!r._id) { wx.showToast({ title: '记录信息缺失，请重新生成', icon: 'none' }); return }

    this.setData({ posterLoading: true })
    try {
      // ① 探缓存（命中就不生图，省十几秒、图固定）
      const probe = await wx.cloud.callFunction({
        name: 'genPosterImage',
        data: {
          action: 'getToday',
          recordId: r._id,
          constellation: this.data.constellations[this.data.constellationIdx],
          persona: r.persona
        }
      })
      const p = probe.result
      if (p.code === 'CACHED') {
        wx.previewImage({ urls: [p.posterFileID], current: p.posterFileID })
        return
      }
      if (p.code !== 'OK') throw new Error(p.msg || '生图失败')

      // ② 下背景图（downloadFile 不走 promise 化，用 success/fail 包一层）
      const dl = await new Promise((resolve, reject) => {
        wx.downloadFile({ url: p.url, success: resolve, fail: reject })
      })
      if (dl.statusCode !== 200 || !dl.tempFilePath) {
        throw new Error('下载失败（HTTP ' + dl.statusCode + '）')
      }

      // ③ Canvas 合成（drawPoster 不动）
      const posterPath = await this.drawPoster(dl.tempFilePath)

      // ④ 上传云存储（cloudPath 用 recordId，当天同名覆盖）
      const up = await wx.cloud.uploadFile({
        cloudPath: 'posters/' + r._id + '.png',
        filePath: posterPath
      })

      // ⑤ 回写 fileID（best-effort：失败不阻断预览，下次重走生图）
      try {
        await wx.cloud.callFunction({
          name: 'genPosterImage',
          data: { action: 'save', recordId: r._id, fileID: up.fileID }
        })
      } catch (e) {
        console.warn('poster writeback failed', e)
      }

      // ⑥ 预览（wx.previewImage 原生支持 cloud:// fileID，自带保存到相册）
      wx.previewImage({ urls: [up.fileID], current: up.fileID })
    } catch (e) {
      wx.showToast({ title: '海报失败：' + (e.errMsg || e.message), icon: 'none' })
    } finally {
      this.setData({ posterLoading: false })
    }
  }
```

> `drawPoster` / `wrapText` 完全不动；`onShow`/`generate` 等其他方法不动。

- [ ] **Step 3: home.wxml — 删自画预览层**

删掉末尾这整块（保留 canvas 和按钮）：
```xml
  <!-- 海报预览层：长按图片 → 微信原生"保存到相册"菜单 -->
  <view wx:if="{{posterVisible}}" class="poster-mask" bindtap="closePoster">
    <image src="{{posterTempPath}}" mode="widthFix" show-menu-by-longpress="true" class="poster-img" catchtap="" />
    <view class="poster-tip">📷 长按图片可保存到相册</view>
  </view>
```

- [ ] **Step 4: home.wxss — 删预览层样式**

删掉这三段（保留 `.poster-canvas` / `.poster-btn`）：
```css
.poster-mask { ... }
.poster-img { ... }
.poster-tip { ... }
```

- [ ] **Step 5: 提交**
```bash
git add miniprogram/pages/home
git commit -m "feat: 海报改走云存储缓存 + wx.previewImage，去掉自画预览层"
```

**验证：** 首页生成人设 → 点「生成海报」→ 等十几秒弹出全屏预览（长按可保存）；关掉再点 → **秒开同一张**（CACHED，无等待）。控制台无 `posterTempPath`/`posterVisible` 报错。

---

## Task 4: history 加点击预览

**Files:**
- Modify: `miniprogram/pages/history/history.wxml`
- Modify: `miniprogram/pages/history/history.js`

- [ ] **Step 1: history.wxml — card 加 bindtap，把整条 record 传过去**

```xml
  <view wx:for="{{records}}" wx:key="_id" class="card"
        bindtap="onTapRecord" data-record="{{item}}">
    <view class="date">{{item.date}}</view>
    <view class="persona">🎭 {{item.persona}}</view>
    <view class="fortune">{{item.fortune}}</view>
    <view class="score">运势 {{item.luckScore}}/100</view>
  </view>
```

- [ ] **Step 2: history.js — 加 onTapRecord**

在 `Page({ ... })` 里加：
```js
  // 点历史记录：有海报就预览，没有就提示
  onTapRecord(e) {
    const rec = e.currentTarget.dataset.record
    if (rec && rec.posterFileID) {
      wx.previewImage({ urls: [rec.posterFileID], current: rec.posterFileID })
    } else {
      wx.showToast({ title: '当天未生成海报', icon: 'none' })
    }
  },
```

> `loadHistory` 不动——查询已返回完整 doc，`posterFileID` 自动带回。

- [ ] **Step 3: 提交**
```bash
git add miniprogram/pages/history
git commit -m "feat: 历史记录点开预览当天海报，无图占位提示"
```

**验证：** history tab 里点一条**已生成海报**的记录 → 弹出同一张海报；点一条**没海报**的旧记录 → toast「当天未生成海报」。

---

## Task 5: 部署 + 权限确认 + 端到端联调

- [ ] **Step 1: 确认两个云函数都已部署**

- `generatePersona`（Task 1 改的）→ 上传部署。
- `genPosterImage`（Task 2 改的）→ **上传并部署：云端安装依赖**（加了 wx-server-sdk）。

- [ ] **Step 2: 确认云存储权限**

微信开发者工具 → 云开发控制台 → 存储 → 权限设置 → 选「**所有用户可读，仅创建者可写**」（默认即此）。

- [ ] **Step 3:（真机前必做）配置 downloadFile 合法域名**

上传走 `wx.cloud.uploadFile`（不需合法域名）；但**下 COS 背景图**仍是普通 `wx.downloadFile`，真机会被拦。去小程序后台 → 开发管理 → 服务器域名 → downloadFile 合法域名，加：
```
https://hunyuan-base-prod-1258344703.cos.ap-guangzhou.myqcloud.com
```

- [ ] **Step 4: 端到端测试**

1. **当天首次 + 复用**：首页登录 → 生成人设 → 点「生成海报」→ 等 15~30s 弹预览。关掉再点 → **秒开同一张**。控制台看 `posters/{recordId}.png` 存在、records 该条有 `posterFileID`。
2. **历史回看**：history tab 看到该条 → 点 card → 弹同一张海报（fileID 与首页一致）。
3. **无图占位**：找/造一条无 `posterFileID` 的旧记录 → 点 → toast「当天未生成海报」。
4. **回写容忍**：临时让 save 分支抛错（或断网那一步）→ 上传成功、预览照常、下次重走生图。
5. **删层回归**：全局搜 `posterTempPath`/`posterVisible`/`closePoster`/`.poster-mask` 应无残留。

- [ ] **Step 5: 提交收尾**（如有遗留改动）
```bash
git add -A
git commit -m "chore: 海报云存储功能联调完成"
```

**验证：** 全流程跑通：生成海报 → 当天复用 → 历史回看 → 无图占位。🎉

---

## 常见问题排查

- **第二次点没秒开、又生图了**：检查首次的 save 是否成功（数据库该条有没有 `posterFileID`）；`recordId` 是否对得上；`_openid` 是否匹配（云函数 `where {_id, _openid}` 双校验）。
- **`wx.previewImage` 不显示 cloud:// 图**：确认基础库 ≥ 2.2.3（当前 `libVersion 3.3.0`，没问题）；极个别旧微信客户端不识别 cloud:// 时，fallback 是云函数调 `cloud.getTempFileURL` 换 https URL 再预览（先不写，YAGNI）。
- **`wx.cloud.uploadFile` 失败**：网络问题重试；若提示体积超限（10MB），回退 `drawPoster` 的 `destWidth = W`（不按 dpr 放大）。
- **历史点开提示"未生成"但明明生成过**：那条记录是改本功能**之前**生成的老数据，没 `posterFileID`，属正常（按决策只占位提示）。
- **跨设备同账号看不到海报**：不可能——`_openid` 相同，`where {_id, _openid}` 命中，应 CACHED。检查是不是另一个微信号。

## 备注 / 风险

- **云存储配额**：每用户每天 1 张 PNG（几 MB），长期累积会吃免费配额（约 5GB）。短期可接受，中期需规划清理 / 迁移 COS。
- **AI URL 24h 失效**：前端拿到 url 后立即 downloadFile；用户切后台太久导致过期时，downloadFile 失败提示「请重新点击生成」。
- **可选优化（本次不做）**：drawPoster 右下角日期改用 `result.date`（服务端 Asia/Shanghai 当日），避免客户端时区错位；本次保持 `new Date()`。
